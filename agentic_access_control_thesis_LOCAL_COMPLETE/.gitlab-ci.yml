image: python:3.12-slim

stages:
  - test

before_script:
  - python -m pip install --upgrade pip
  - pip install -e ".[dev]"

test:
  stage: test
  script:
    - pytest -q
    - ace validate-detector
    - ace run --backend mock --repetitions 1 --mock-error-rate 0 --output-dir outputs/ci-smoke
